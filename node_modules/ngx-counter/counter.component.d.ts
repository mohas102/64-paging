import { EventEmitter, OnInit } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
export declare class CounterComponent implements ControlValueAccessor, OnInit {
    change: EventEmitter<number>;
    font: string;
    theme: string;
    color: string;
    bgcolor: string;
    size: string;
    speed: number;
    limit: number;
    private _countValue;
    countValue: number;
    blink: boolean;
    circuit: number;
    private timer;
    constructor();
    ngOnInit(): void;
    writeValue(value: any): void;
    private propagateChange;
    registerOnChange(fn: any): void;
    registerOnTouched(): void;
    getColor(): string;
}
